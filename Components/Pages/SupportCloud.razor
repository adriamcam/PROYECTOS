@page "/support-cloud"

<PageTitle>Support Cloud - ITQS SOC</PageTitle>

<section class="cloud-layout">

    <aside class="cloud-sidebar">
        <div class="cloud-brand">
            <span>☁</span>
            <strong>Support Cloud</strong>
        </div>

        <nav class="cloud-menu">

            <div class="menu-group">
                <button type="button"
                        class="menu-title menu-button"
                        @onclick="SelectAlertMonitoring">
                    🔔 Monitoreo de Alertas
                </button>

                <div class="menu-links @(ShowAlertMenu ? "show" : "")">
                    <button type="button" class="menu-link" @onclick='() => CurrentModule = "Alertas"'>Dashboard</button>
                    <button type="button" class="menu-link" @onclick='() => CurrentModule = "Alertas"'>Asignadas a mí</button>
                    <button type="button" class="menu-link" @onclick='() => CurrentModule = "Alertas"'>Sin asignar</button>
                    <button type="button" class="menu-link" @onclick='() => CurrentModule = "Alertas"'>Todas las alertas</button>
                    <button type="button" class="menu-link" @onclick='() => CurrentModule = "Alertas"'>Alertas críticas</button>
                    <button type="button" class="menu-link" @onclick='() => CurrentModule = "Clientes"'>Info Clientes</button>
                    <button type="button" class="menu-link" @onclick='() => CurrentModule = "Inventory"'>Inventory</button>
                    <button type="button" class="menu-link" @onclick='() => CurrentModule = "Admin"'>Admin / Manager View</button>
                </div>
            </div>

        </nav>

        <a class="cloud-logout" href="/portal">← Volver al Portal</a>
    </aside>

    <main class="cloud-main">
        <header class="cloud-topbar">
            <div class="hamburger">☰</div>
            <strong>Support Cloud</strong>

            <div class="cloud-user">
                <div class="avatar">A</div>
                <div>
                    <strong>Administrador</strong>
                    <span>ITQS SOC</span>
                </div>
            </div>
        </header>

        <section class="cloud-content">

            @if (CurrentModule == "Alertas")
            {
                <h1>Monitoreo de Alertas</h1>
                <p>Monitoreo centralizado de alertas, asignaciones y operación.</p>

                <div class="cloud-divider"></div>

                <div class="kpi-row">

                    <article class="kpi-box">
                        <h3>Alertas Totales</h3>
                        <div class="kpi-icon blue">🔔</div>
                        <strong>146</strong>
                        <span>Activas</span>
                    </article>

                    <article class="kpi-box">
                        <h3>Backup Alerts</h3>
                        <div class="kpi-icon purple">💾</div>
                        <strong>42</strong>
                        <span>Activas</span>
                    </article>

                    <article class="kpi-box">
                        <h3>Management Alerts</h3>
                        <div class="kpi-icon orange">⚙</div>
                        <strong>71</strong>
                        <span>Activas</span>
                    </article>

                    <article class="kpi-box">
                        <h3>Asignadas a mí</h3>
                        <div class="kpi-icon green">👤</div>
                        <strong>28</strong>
                        <span>Pendientes</span>
                    </article>

                    <article class="kpi-box">
                        <h3>Sin asignar</h3>
                        <div class="kpi-icon red">⚠</div>
                        <strong>34</strong>
                        <span>Pendientes</span>
                    </article>

                    <article class="kpi-box">
                        <h3>Resueltas hoy</h3>
                        <div class="kpi-icon purple">✓</div>
                        <strong>16</strong>
                        <span>Cerradas</span>
                    </article>

                </div>
            }
            else if (CurrentModule == "Clientes")
            {
                <h1>Info Clientes</h1>
                <p>Información de clientes, tenants, suscripciones y contactos operativos.</p>
                <div class="cloud-divider"></div>
            }
            else if (CurrentModule == "Inventory")
            {
                <h1>Inventory</h1>
                <p>Inventario Azure multi-tenant de recursos, servidores y servicios.</p>
                <div class="cloud-divider"></div>
            }
            else if (CurrentModule == "Admin")
            {
                <h1>Admin / Manager View</h1>
                <p>Administración, configuración, usuarios, reglas y gestión operativa.</p>
                <div class="cloud-divider"></div>
            }
            else
            {
                <h1>Support Cloud</h1>
                <p>Seleccione un módulo del menú lateral.</p>
                <div class="cloud-divider"></div>
            }

        </section>
    </main>

</section>

@code {
    private string CurrentModule = "";
    private bool ShowAlertMenu = false;

    private void SelectAlertMonitoring()
    {
        CurrentModule = "Alertas";
        ShowAlertMenu = !ShowAlertMenu;
    }
}