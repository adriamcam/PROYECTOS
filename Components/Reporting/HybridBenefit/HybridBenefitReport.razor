@namespace ITQS.SupportOperationsCenter.Components.Reporting.HybridBenefit

@using ITQS.SupportOperationsCenter.Services
@using ITQS.SupportOperationsCenter.Components.Reporting.HybridBenefit.Models
@using ITQS.SupportOperationsCenter.Components.Reporting.HybridBenefit.Components

@inject IHybridBenefitService HybridBenefitService

<section class="hb-page">

    @if (IsLoading)
    {
        <section class="hb-card">
            <strong>Cargando Reporte Beneficio Híbrido...</strong>    @if (ShowInfoPopup)
    {
        <div class="report-modal-backdrop">
            <div class="report-info-modal">
                <div class="modal-header">
                    <div>
                        <span class="report-eyebrow">INFORMACIÓN TÉCNICA</span>
                        <h2>Objetos utilizados por el reporte</h2>
                    </div>

                    <button type="button" class="modal-close" @onclick="() => ShowInfoPopup = false">×</button>
                </div>

                <div class="info-grid">
                    <div class="info-box">
                        <h3>Tablas</h3>
                        <ul>
                            <li>dbo.PartnerCenterCustomers</li>
                            <li>dbo.CRM_Customers</li>
                            <li>dbo.HybridBenefitResources</li>
                            <li>dbo.HybridBenefitHistory</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Vistas</h3>
                        <ul>
                            <li>dbo.vw_HybridBenefit_Dashboard</li>
                            <li>dbo.vw_HybridBenefit_Resources</li>
                            <li>dbo.vw_HybridBenefit_Changes</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Procedimientos</h3>
                        <ul>
                            <li>dbo.usp_HybridBenefit_Sync</li>
                            <li>dbo.usp_HybridBenefit_RegisterChange</li>
                            <li>dbo.usp_HybridBenefit_ExportChanges</li>
                        </ul>
                    </div>
                </div>

                <div class="info-note">
                    <strong>Objetivo:</strong>
                    monitorear VMs con Azure Hybrid Benefit, validar cambios de estado, detectar altas/bajas,
                    controlar tags y permitir exportar cambios a Excel.
                </div>
            </div>
        </div>
    }
</section>
    }
    else if (!string.IsNullOrWhiteSpace(ErrorMessage))
    {
        <section class="hb-card">
            <strong>Error cargando datos</strong>
            <p>@ErrorMessage</p>    @if (ShowInfoPopup)
    {
        <div class="report-modal-backdrop">
            <div class="report-info-modal">
                <div class="modal-header">
                    <div>
                        <span class="report-eyebrow">INFORMACIÓN TÉCNICA</span>
                        <h2>Objetos utilizados por el reporte</h2>
                    </div>

                    <button type="button" class="modal-close" @onclick="() => ShowInfoPopup = false">×</button>
                </div>

                <div class="info-grid">
                    <div class="info-box">
                        <h3>Tablas</h3>
                        <ul>
                            <li>dbo.PartnerCenterCustomers</li>
                            <li>dbo.CRM_Customers</li>
                            <li>dbo.HybridBenefitResources</li>
                            <li>dbo.HybridBenefitHistory</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Vistas</h3>
                        <ul>
                            <li>dbo.vw_HybridBenefit_Dashboard</li>
                            <li>dbo.vw_HybridBenefit_Resources</li>
                            <li>dbo.vw_HybridBenefit_Changes</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Procedimientos</h3>
                        <ul>
                            <li>dbo.usp_HybridBenefit_Sync</li>
                            <li>dbo.usp_HybridBenefit_RegisterChange</li>
                            <li>dbo.usp_HybridBenefit_ExportChanges</li>
                        </ul>
                    </div>
                </div>

                <div class="info-note">
                    <strong>Objetivo:</strong>
                    monitorear VMs con Azure Hybrid Benefit, validar cambios de estado, detectar altas/bajas,
                    controlar tags y permitir exportar cambios a Excel.
                </div>
            </div>
        </div>
    }
</section>
    }
    else
    {
        <div class="hb-hero">
            <div>
                <span class="hb-eyebrow">Reportería</span>
                <h1>Reporte Beneficio Híbrido</h1>
                <p>Monitoreo y análisis del uso de Azure Hybrid Benefit por cliente, suscripción y recurso.</p>

                <div class="hb-meta">
                    <span>Última ejecución</span>
                    <strong>@Dashboard.Kpis.LastScanDate.ToString("dd/MM/yyyy HH:mm")</strong>
                </div>
            </div>

            <div class="hb-actions">
                <button type="button" class="hb-btn hb-btn-primary" @onclick="LoadDashboardAsync">
                    Actualizar
                </button>
            </div>
        </div>

        <div class="hb-tabs">
            <button class="@(ActiveTab == "Dashboard" ? "active" : "")" @onclick='() => SetTab("Dashboard")'>
                Dashboard
            </button>

            <button class="@(ActiveTab == "Resources" ? "active" : "")" @onclick='() => SetTab("Resources")'>
                Azure Hybrid Benefit Resources
            </button>
        </div>

        @if (ActiveTab == "Dashboard")
        {
            <HBKpis Dashboard="Dashboard" />

            <div class="hb-dashboard-grid">
                <section class="hb-card hb-card-large">
                    <div class="hb-card-header">
                        <div>
                            <h3>Top clientes con AHUB</h3>
                            <p>Clientes con mayor cantidad de recursos usando beneficio híbrido.</p>
                        </div>
                    </div>

                    <div class="hb-bars">
                        @foreach (var item in Dashboard.TopCustomers.Take(5))
                        {
                            var max = Dashboard.TopCustomers.Any() ? Dashboard.TopCustomers.Max(x => x.TotalResources) : 1;
                            var width = Math.Max(12, (int)Math.Round((item.TotalResources * 100.0) / max));

                            <div>
                                <span>@item.Customer</span>
                                <strong style="width:@width%">@item.TotalResources</strong>
                            </div>
                        }
                    </div>    @if (ShowInfoPopup)
    {
        <div class="report-modal-backdrop">
            <div class="report-info-modal">
                <div class="modal-header">
                    <div>
                        <span class="report-eyebrow">INFORMACIÓN TÉCNICA</span>
                        <h2>Objetos utilizados por el reporte</h2>
                    </div>

                    <button type="button" class="modal-close" @onclick="() => ShowInfoPopup = false">×</button>
                </div>

                <div class="info-grid">
                    <div class="info-box">
                        <h3>Tablas</h3>
                        <ul>
                            <li>dbo.PartnerCenterCustomers</li>
                            <li>dbo.CRM_Customers</li>
                            <li>dbo.HybridBenefitResources</li>
                            <li>dbo.HybridBenefitHistory</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Vistas</h3>
                        <ul>
                            <li>dbo.vw_HybridBenefit_Dashboard</li>
                            <li>dbo.vw_HybridBenefit_Resources</li>
                            <li>dbo.vw_HybridBenefit_Changes</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Procedimientos</h3>
                        <ul>
                            <li>dbo.usp_HybridBenefit_Sync</li>
                            <li>dbo.usp_HybridBenefit_RegisterChange</li>
                            <li>dbo.usp_HybridBenefit_ExportChanges</li>
                        </ul>
                    </div>
                </div>

                <div class="info-note">
                    <strong>Objetivo:</strong>
                    monitorear VMs con Azure Hybrid Benefit, validar cambios de estado, detectar altas/bajas,
                    controlar tags y permitir exportar cambios a Excel.
                </div>
            </div>
        </div>
    }
</section>

                <section class="hb-card">
                    <div class="hb-card-header">
                        <div>
                            <h3>Distribución AHUB</h3>
                            <p>Clasificación de beneficios Azure Hybrid Benefit.</p>
                        </div>
                    </div>

                    <div class="hb-change-list">
                        @foreach (var item in Dashboard.Distribution)
                        {
                            <div>
                                <strong>@item.BenefitType</strong>
                                <span>@item.Total</span>
                            </div>
                        }
                    </div>    @if (ShowInfoPopup)
    {
        <div class="report-modal-backdrop">
            <div class="report-info-modal">
                <div class="modal-header">
                    <div>
                        <span class="report-eyebrow">INFORMACIÓN TÉCNICA</span>
                        <h2>Objetos utilizados por el reporte</h2>
                    </div>

                    <button type="button" class="modal-close" @onclick="() => ShowInfoPopup = false">×</button>
                </div>

                <div class="info-grid">
                    <div class="info-box">
                        <h3>Tablas</h3>
                        <ul>
                            <li>dbo.PartnerCenterCustomers</li>
                            <li>dbo.CRM_Customers</li>
                            <li>dbo.HybridBenefitResources</li>
                            <li>dbo.HybridBenefitHistory</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Vistas</h3>
                        <ul>
                            <li>dbo.vw_HybridBenefit_Dashboard</li>
                            <li>dbo.vw_HybridBenefit_Resources</li>
                            <li>dbo.vw_HybridBenefit_Changes</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Procedimientos</h3>
                        <ul>
                            <li>dbo.usp_HybridBenefit_Sync</li>
                            <li>dbo.usp_HybridBenefit_RegisterChange</li>
                            <li>dbo.usp_HybridBenefit_ExportChanges</li>
                        </ul>
                    </div>
                </div>

                <div class="info-note">
                    <strong>Objetivo:</strong>
                    monitorear VMs con Azure Hybrid Benefit, validar cambios de estado, detectar altas/bajas,
                    controlar tags y permitir exportar cambios a Excel.
                </div>
            </div>
        </div>
    }
</section>

                <section class="hb-card">
                    <div class="hb-card-header">
                        <div>
                            <h3>Cambios recientes</h3>
                            <p>Activaciones y desactivaciones detectadas.</p>
                        </div>
                    </div>

                    <div class="hb-change-list">
                        @if (Dashboard.Changes.Any())
                        {
                            @foreach (var item in Dashboard.Changes.Take(5))
                            {
                                <div>
                                    <strong>@(item.AHUB_Status ?? item.ChangeType ?? "Cambio")</strong>
                                    <span>@item.ResourceName</span>
                                </div>
                            }
                        }
                        else
                        {
                            <div>
                                <strong>Sin cambios recientes</strong>
                                <span>0</span>
                            </div>
                        }
                    </div>    @if (ShowInfoPopup)
    {
        <div class="report-modal-backdrop">
            <div class="report-info-modal">
                <div class="modal-header">
                    <div>
                        <span class="report-eyebrow">INFORMACIÓN TÉCNICA</span>
                        <h2>Objetos utilizados por el reporte</h2>
                    </div>

                    <button type="button" class="modal-close" @onclick="() => ShowInfoPopup = false">×</button>
                </div>

                <div class="info-grid">
                    <div class="info-box">
                        <h3>Tablas</h3>
                        <ul>
                            <li>dbo.PartnerCenterCustomers</li>
                            <li>dbo.CRM_Customers</li>
                            <li>dbo.HybridBenefitResources</li>
                            <li>dbo.HybridBenefitHistory</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Vistas</h3>
                        <ul>
                            <li>dbo.vw_HybridBenefit_Dashboard</li>
                            <li>dbo.vw_HybridBenefit_Resources</li>
                            <li>dbo.vw_HybridBenefit_Changes</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Procedimientos</h3>
                        <ul>
                            <li>dbo.usp_HybridBenefit_Sync</li>
                            <li>dbo.usp_HybridBenefit_RegisterChange</li>
                            <li>dbo.usp_HybridBenefit_ExportChanges</li>
                        </ul>
                    </div>
                </div>

                <div class="info-note">
                    <strong>Objetivo:</strong>
                    monitorear VMs con Azure Hybrid Benefit, validar cambios de estado, detectar altas/bajas,
                    controlar tags y permitir exportar cambios a Excel.
                </div>
            </div>
        </div>
    }
</section>
            </div>
        }
        else if (ActiveTab == "Resources")
        {
            <HBResourcesTable Resources="Dashboard.Resources" />
        }
    }    @if (ShowInfoPopup)
    {
        <div class="report-modal-backdrop">
            <div class="report-info-modal">
                <div class="modal-header">
                    <div>
                        <span class="report-eyebrow">INFORMACIÓN TÉCNICA</span>
                        <h2>Objetos utilizados por el reporte</h2>
                    </div>

                    <button type="button" class="modal-close" @onclick="() => ShowInfoPopup = false">×</button>
                </div>

                <div class="info-grid">
                    <div class="info-box">
                        <h3>Tablas</h3>
                        <ul>
                            <li>dbo.PartnerCenterCustomers</li>
                            <li>dbo.CRM_Customers</li>
                            <li>dbo.HybridBenefitResources</li>
                            <li>dbo.HybridBenefitHistory</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Vistas</h3>
                        <ul>
                            <li>dbo.vw_HybridBenefit_Dashboard</li>
                            <li>dbo.vw_HybridBenefit_Resources</li>
                            <li>dbo.vw_HybridBenefit_Changes</li>
                        </ul>
                    </div>

                    <div class="info-box">
                        <h3>Procedimientos</h3>
                        <ul>
                            <li>dbo.usp_HybridBenefit_Sync</li>
                            <li>dbo.usp_HybridBenefit_RegisterChange</li>
                            <li>dbo.usp_HybridBenefit_ExportChanges</li>
                        </ul>
                    </div>
                </div>

                <div class="info-note">
                    <strong>Objetivo:</strong>
                    monitorear VMs con Azure Hybrid Benefit, validar cambios de estado, detectar altas/bajas,
                    controlar tags y permitir exportar cambios a Excel.
                </div>
            </div>
        </div>
    }
</section>

@code {
    private HybridBenefitDashboard Dashboard { get; set; } = new();
    private bool IsLoading { get; set; } = true;
    private string? ErrorMessage { get; set; }
    private string ActiveTab { get; set; } = "Dashboard";

    protected override async Task OnInitializedAsync()
    {
        await LoadDashboardAsync();
    }

    private void SetTab(string tab)
    {
        ActiveTab = tab;
    }

    private async Task LoadDashboardAsync()
    {
        try
        {
            IsLoading = true;
            ErrorMessage = null;

            Dashboard = await HybridBenefitService.GetDashboardAsync();
        }
        catch (Exception ex)
        {
            ErrorMessage = ex.Message;
        }
        finally
        {
            IsLoading = false;
        }
    }
}


@code {
    private bool ShowInfoPopup = false;
}


Components\Reporting\HybridBenefit\HybridBenefitReport.razor=Components\Reporting\HybridBenefit\HybridBenefitReport.razor.css


